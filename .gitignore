.DS_Store
*.zip
dist/
node_modules/
package-lock.json
package.json
test/shot-*.png
fixtures/stroke_frames/
clips/
out/
# vendor/ is NOT ignored: src/app.js loads vendor/opencv.js by a hard-coded
# path, so ignoring it made every fresh clone fall back to appearance-only
# detection with nothing but a transient toast to say why.
